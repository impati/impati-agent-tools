# 변경 기록

최신 변경부터 기록한다. 각 항목은 변경 의도와 핵심 내용, 중요한 결정만 간결하게 남긴다.

## 2026-08-15 — 전역 사용을 위한 플러그인 배포 구조 추가

- **의도:** 저장소의 공통 작업 원칙과 스킬을 다른 프로젝트에서 시작한 Codex 세션에도 일관되게 적용한다.
- **변경:** 저장소를 로컬 마켓플레이스로 구성하고, 기존 원칙과 네 가지 스킬을 `impati-codex-tools` 플러그인의 단일 원본으로 옮겼으며 `SessionStart` 훅으로 공통 원칙을 주입한다.
- **결정:** 플러그인 훅은 사용자가 신뢰한 뒤 모든 세션 시작·재개·초기화·컨텍스트 압축 시 실행하고, 대상 프로젝트의 더 구체적인 로컬 규칙을 우선한다.

## 2026-08-15 — 로컬 IDE 설정과 중복 문서 정리

- **의도:** IntelliJ의 개인 로컬 설정이 저장소에 포함되지 않게 하고, 스킬과 중복되는 하위 안내 문서를 줄인다.
- **변경:** `.idea/`를 루트 `.gitignore`에 추가하고 `docs/backlog/README.md`를 제거했다.
- **결정:** 백로그 규약은 `capture-backlog` 스킬에서 관리하며 실제 백로그 항목만 `docs/backlog`에 둔다.

## 2026-08-15 — RCA 다관점 코드 리뷰 추가

- **의도:** 작업 중 놓친 원인, 영향 전파와 재발 가능성을 커밋별·누적 관점에서 검토한다.
- **변경:** 명시적 요청에만 실행되고 결과를 채팅으로 보고하는 `rca-code-review` 스킬을 추가했다.
- **결정:** 각 스킬은 다음 단계를 안내만 하며 자동 실행하지 않는다. 백로그는 작업 시작, 작업 완료는 코드 리뷰, 리뷰 후 별도 과제는 백로그 기록을 안내한다.

## 2026-08-15 — 백로그 기반 코드 추적 규칙 추가

- **의도:** 백로그, 작업 브랜치, 커밋과 코드 리뷰를 하나의 고유 번호로 연결한다.
- **변경:** `BL-NNNN` 순차 ID를 도입하고 모든 기능 코드 작업에 백로그 연결과 규칙적인 feature 브랜치를 필수화했다.
- **결정:** 기본 브랜치는 `feature/bl-NNNN-title`, 기본 커밋은 `<type>(bl-NNNN): <한국어 요약>` 형식을 사용하되 로컬 프로젝트 규칙을 우선한다.

## 2026-08-15 — 가벼운 백로그 수집 도입

- **의도:** 문제를 인지한 시점과 실제 작업 시점이 달라도 일정한 위치에 배경과 목표를 남긴다.
- **변경:** 항목별 파일을 `docs/backlog`에 기록하는 `capture-backlog` 스킬을 추가하고, `start-work` 자동 실행을 기능 동작에 영향을 주는 코드 작업으로 좁혔다.
- **결정:** 백로그에는 상태를 두지 않으며, 이 하네스 프로젝트에서는 ADR을 제거하고 과거 결정은 변경 로그로만 보존한다.

## 2026-08-15 — 리뷰 단위 커밋 절차 추가

- **의도:** 사용자가 작업 결과를 커밋 단위로 이해하고 리뷰할 수 있게 한다.
- **변경:** `start-work`에 커밋 계획, 안전한 스테이징, 논리 단위 커밋과 커밋별 완료 보고 절차를 추가했다.
- **결정:** 로컬 커밋 규칙을 우선하고, 별도 규칙이 없으면 Conventional Commits를 사용하며 push·브랜치·PR은 자동 수행하지 않는다.

## 2026-08-15 — 로컬 프로젝트 규칙 우선

- **의도:** 공통 하네스를 사용하더라도 프로젝트마다 다른 환경과 작업 관례를 보존한다.
- **변경:** 하네스와 대상 프로젝트의 규칙이 충돌하면 더 구체적인 로컬 규칙을 우선하도록 `AGENTS.md`에 우선순위를 추가했다.
- **결정:** 하네스는 기본값으로 취급하되 시스템·안전 정책과 현재 사용자의 명시적 요청은 로컬 설정이 덮어쓰지 않는다.

## 2026-08-15 — 작업 시작 인터뷰와 ADR 도입

- **의도:** 개발 전에 사용자와 컨텍스트를 충분히 맞추고, 주요 결정을 사용자 승인 아래 확정한다.
- **변경:** 변경 작업 전에 상세 인터뷰를 수행하는 `start-work` 스킬과 자동 실행 규칙을 추가하고, 승인 결과를 `docs/adr`에 남기는 규약을 마련했다.
- **결정:** 결과에 영향을 주는 결정을 우선 질문하되 경계는 실제 사용으로 조정하며, 구현 중 새 결정이 생겨도 사용자 확인 후 진행한다.

## 2026-08-15 — 초기 작업 환경 구성

- **의도:** 개인 작업 원칙과 반복 작업용 스킬을 한 저장소에서 관리하고, 이 저장소로 시작한 Codex 세션에 자동으로 제공한다.
- **변경:** 사용자 의도와 요청 범위를 우선하는 `AGENTS.md`, 로컬 `engineering-writeup` 스킬의 저장소 사본과 UI 메타데이터, 사용 방법을 설명하는 `README.md`를 추가했다.
- **결정:** 변경 기록은 당분간 단일 `CHANGELOG.md`에 간결하게 누적하고, 메이저 변경이나 문서 비대화가 발생한 뒤 분리를 검토한다.
