# 변경 기록

최신 변경부터 기록한다. 각 항목은 변경 의도와 핵심 내용, 중요한 결정만 간결하게 남긴다.

## 0.7.0 — 2026-08-17 — 비즈니스 정책 결정 문서 추가

- **의도:** 기술 결정을 담는 ADR은 있는데 사람이 정해야 하는 비즈니스 규칙을 담을 자리가 없어, 정책이 코드 주석과 문서 곳곳으로 흩어지고 정본이 사라진다. (BL-0003)
- **변경:** 규칙을 인터뷰로 확정해 `docs/policy`에 `PD-NNNN` 결정 문서로 남기는 `decide-policy` 스킬과 정책 문서 규약을 추가했다. `start-work`는 결정 지도 단계에서 정책 결정 필요 여부를 판정해 제안한다.
- **결정:** 정책 문서는 무엇이 참이어야 하는지를, ADR은 그것을 어떤 방식으로 구현할지를 담는다. 둘 다 필요할 때만 만들며 스킬의 첫 단계가 필요 여부 판정이다. 결정은 시점을 고정해 뒤집을 때 새 번호로 대체하고, "지금 유효한 규칙 전부"를 주장하는 카탈로그 문서는 만들지 않는다. 지금 무엇이 참인지는 명세 테스트가 답하며, 규칙 단위 `PD-NNNN-RN` ID를 테스트 주석에 표기해 둘을 잇는다. 테스트 작성은 기능 코드 작업이므로 `start-work`에 남긴다.
- **적용 범위:** 하네스를 적용한 프로젝트 전체에 적용한다. 대상 저장소에 다른 정책 문서 관례가 있으면 그 관례를 우선한다.
- **보완:** 새 스킬을 추가할 때 `.agents/skills` 탐색 링크가 조용히 누락되던 문제를 막기 위해 Codex `sync-plugin.py`가 이 링크를 스킬 원본에 맞춰 생성하고 `--check`로 검증한다. 정책·ADR 규약의 번호 충돌 처리 문구도 같은 기준으로 맞췄다.

## 0.6.0 — 2026-08-16 — 완료 백로그를 보관소로 분리

- **의도:** 백로그 항목이 쌓이면 한 디렉터리에서 무엇이 남았는지 구분할 수 없어, 열린 항목을 한눈에 보는 비용이 계속 커진다.
- **변경:** `docs/backlog`는 열린 항목만 두고 완료 항목은 `docs/backlog/done`으로 옮긴다. `start-work`가 완료 기준 검증을 통과한 뒤 `git mv`로 자동 이동하고 그 변경을 마지막 커밋에 포함한다. `capture-backlog`의 중복 확인과 `BL-NNNN` 채번, `rca-code-review`의 항목 조회가 두 위치를 모두 본다.
- **결정:** 상태는 위치로만 구분하고 별도 상태 필드나 3단계 디렉터리는 두지 않는다. 전이 지점을 완료 1회로 제한해 상태가 어긋날 여지를 줄인다. 검증 실패나 중단 시에는 옮기지 않으며, 완료 항목은 삭제하지 않고 채번 대상에 계속 포함해 번호 재사용을 막는다.
- **적용 범위:** 하네스를 적용한 프로젝트 전체에 적용한다. 대상 저장소에 다른 백로그 관례가 있으면 그 관례를 우선한다.

## 0.5.2 — 2026-08-16 — 다른 프로젝트 대상 로컬 설치 수정

- **의도:** `local` 범위로 다른 프로젝트에 설치하려는 흐름이 실제로는 하네스 저장소에 설치되거나 버전이 갱신되지 않아, 적용 프로젝트에 최신 규칙이 반영되지 않았다.
- **변경:** `update-local-plugin.py`에 대상 프로젝트를 지정하는 `--project-dir`를 추가하고 설치·조회 명령을 그 디렉터리에서 실행한다. 이미 설치된 경우 제거 후 재설치하며, 설치 후 버전뿐 아니라 범위와 대상 프로젝트 경로까지 검증한다.
- **결정:** `claude plugin install`이 기존 설치를 no-op으로 넘기는 동작을 전제로 재설치를 스크립트 책임으로 둔다. Claude `release-plugin` 스킬과 README에도 같은 절차를 명시했다.

## 0.5.1 — 2026-08-16 — 변경 기록 규칙의 적용 범위를 하네스 저장소로 한정

- **의도:** `CHANGELOG.md` 관리는 이 하네스 저장소의 규약인데, 훅으로 주입된 규칙이 하네스를 적용한 다른 프로젝트에도 변경 기록을 요구했다.
- **변경:** `core/AGENTS.md`의 변경 기록 규칙을 하네스 저장소 전용으로 명시하고, `start-work`가 대상 프로젝트에서 `CHANGELOG.md`를 새로 만들거나 갱신을 요구하지 않도록 완료 절차를 분리했다. ADR 규약의 표현도 저장소가 변경 기록을 관리하는 경우로 한정했다.
- **결정:** 적용 프로젝트의 변경 기록 방식은 그 프로젝트의 로컬 규칙과 사용자의 요청을 따른다.

## 0.5.0 — 2026-08-16 — Claude Code 어댑터 추가

- **의도:** Codex에만 제공하던 공통 작업 원칙과 워크플로를 Claude Code 세션에서도 동일하게 사용한다. (BL-0002)
- **변경:** `adapters/claude`에 `impati-claude-tools` 플러그인, `SessionStart` 훅, Claude 전용 `release-plugin` 스킬과 동기화·검증·재설치 스크립트를 추가하고, 저장소 루트에 Claude Code 전용 `.claude-plugin/marketplace.json`과 `CLAUDE.md` 링크를 두었다.
- **결정:** 코어는 계속 단일 원본으로 두고 어댑터 배포물은 생성물로 관리한다. 저장소는 모든 어댑터에 같은 버전을 사용하며 Codex 플러그인도 0.5.0으로 맞췄다. 스킬 안의 `agents/`는 Codex 인터페이스 전용 메타데이터이므로 Claude 배포물에서 제외한다.
- **공존:** 다른 플러그인도 `SessionStart`로 규칙을 주입할 수 있으므로 설치 범위(`--scope`)와 `claude plugin disable`로 적용 대상을 조절한다.

## 0.4.2 — 2026-08-16 — 문제 해결 기록의 편집 기준 강화

- **의도:** 글을 정확하게 만들기 위해 수집한 작업 근거가 독자가 읽을 문제 해결 서사에 그대로 섞이는 문제를 방지한다.
- **변경:** `engineering-writeup`에 근거 수집과 본문 선별을 분리하는 관련성 기준, 작성 전략을 노출하지 않는 원칙과 최종 편집 단계를 추가했다.
- **결정:** 단순 컴파일·빌드·테스트 성공은 원인, 판단, 해결 또는 배움을 설명할 때만 본문에 포함하며 나머지는 완료 보고 정보로 취급한다.

## 0.4.1 — 2026-08-15 — 문제 해결 중심 기술 기록으로 정교화

- **의도:** 구현 완료 보고서의 고정 목차가 아니라 문제를 이해하고 해결한 과정과 그 안에서 얻은 배움을 독자에게 전달한다.
- **변경:** `engineering-writeup`이 독자 수준에 맞춰 개념을 먼저 설명하고, 실제 인과·의사결정 흐름에 필요한 내용만 자연스러운 구조로 구성하도록 수정했다.
- **결정:** 테스트와 검증은 실제 수행된 경우에만 근거로 다루며 새 확인 방식을 만들지 않는다. PR·MR·게시 흐름은 별도 스킬의 책임으로 분리한다.

## 0.4.0 — 2026-08-15 — 기술 문서 블로그 Draft PR 워크플로 추가

- **의도:** 개발 과정의 근거와 검증 결과를 기존 작성 규칙으로 정리하고, 공개 안전성을 확인한 뒤 블로그 리뷰 단계까지 반복 가능하게 연결한다.
- **변경:** `engineering-writeup`을 재사용해 Chirpy 게시물로 변환하고 격리된 블로그 checkout에서 Draft PR을 만드는 `publish-engineering-writeup` 스킬과 게시물 검증기를 추가했다.
- **결정:** 원본 개발 저장소는 수정하지 않으며 민감정보 검사와 Jekyll 검증을 게시 게이트로 사용하고, 명시적 후속 승인 없이는 머지·Pages 배포를 수행하지 않는다. 이 하네스 저장소 자체는 별도 피처 브랜치 없이 `main`에서 직접 작업한다.

## 0.3.0 — 2026-08-15 — 범용 코어와 에이전트별 어댑터 분리

- **의도:** 작업 원칙과 반복 워크플로를 특정 에이전트에 종속시키지 않고, 필요한 에이전트만 독립 어댑터로 연결한다.
- **변경:** 저장소 정체성을 `impati-agent-tools`로 일반화하고 공통 원칙·스킬을 `core`로, 기존 Codex 플러그인·훅·릴리스 도구를 `adapters/codex`로 재배치했다.
- **결정:** 현재는 Codex 어댑터만 유지하고, 플러그인 배포물은 동기화 스크립트로 공통 코어를 포함하며, 다른 에이전트 어댑터는 요청이 생길 때 추가한다.
- **문서:** `.agents/skills`가 원본을 복제한 폴더가 아니라 스킬 탐색용 심볼릭 링크임을 명시했다.

## 0.2.0 — 2026-08-15 — 버전 릴리스와 로컬 업데이트 절차 추가

- **의도:** 원격에 반영된 플러그인 변경을 버전으로 식별하고 로컬 Codex에 안전하고 반복 가능하게 적용한다.
- **변경:** SemVer 규칙과 `release-plugin` 스킬을 추가하고, 패키지 검증·마켓플레이스 등록·재설치·활성 상태 확인을 수행하는 로컬 업데이트 스크립트를 제공한다.
- **결정:** Git 갱신과 플러그인 설치를 분리하며, 스크립트는 커밋되지 않은 배포물 변경을 덮어쓰지 않고 tag·push도 자동 수행하지 않는다.

## 2026-08-15 — 전역 사용을 위한 플러그인 배포 구조 추가

- **의도:** 저장소의 공통 작업 원칙과 스킬을 다른 프로젝트에서 시작한 Codex 세션에도 일관되게 적용한다.
- **변경:** 저장소를 로컬 마켓플레이스로 구성하고, 기존 원칙과 네 가지 스킬을 `impati-codex-tools` 플러그인의 단일 원본으로 옮겼으며 `SessionStart` 훅으로 공통 원칙을 주입한다.
- **결정:** 플러그인 훅은 사용자가 신뢰한 뒤 모든 세션 시작·재개·초기화·컨텍스트 압축 시 실행하고, 대상 프로젝트의 더 구체적인 로컬 규칙을 우선한다.

## 2026-08-15 — 로컬 IDE 설정과 중복 문서 정리

- **의도:** IntelliJ의 개인 로컬 설정이 저장소에 포함되지 않게 하고, 스킬과 중복되는 하위 안내 문서를 줄인다.
- **변경:** `.idea/`를 루트 `.gitignore`에 추가하고 `docs/backlog/README.md`를 제거했다.
- **결정:** 백로그 규약은 `capture-backlog` 스킬에서 관리하며 실제 백로그 항목만 `docs/backlog`에 둔다.

## 2026-08-15 — RCA 다관점 코드 리뷰 추가

- **의도:** 작업 중 놓친 원인, 영향 전파와 재발 가능성을 커밋별·누적 관점에서 검토한다.
- **변경:** 명시적 요청에만 실행되고 결과를 채팅으로 보고하는 `rca-code-review` 스킬을 추가했다.
- **결정:** 각 스킬은 다음 단계를 안내만 하며 자동 실행하지 않는다. 백로그는 작업 시작, 작업 완료는 코드 리뷰, 리뷰 후 별도 과제는 백로그 기록을 안내한다.

## 2026-08-15 — 백로그 기반 코드 추적 규칙 추가

- **의도:** 백로그, 작업 브랜치, 커밋과 코드 리뷰를 하나의 고유 번호로 연결한다.
- **변경:** `BL-NNNN` 순차 ID를 도입하고 모든 기능 코드 작업에 백로그 연결과 규칙적인 feature 브랜치를 필수화했다.
- **결정:** 기본 브랜치는 `feature/bl-NNNN-title`, 기본 커밋은 `<type>(bl-NNNN): <한국어 요약>` 형식을 사용하되 로컬 프로젝트 규칙을 우선한다.

## 2026-08-15 — 가벼운 백로그 수집 도입

- **의도:** 문제를 인지한 시점과 실제 작업 시점이 달라도 일정한 위치에 배경과 목표를 남긴다.
- **변경:** 항목별 파일을 `docs/backlog`에 기록하는 `capture-backlog` 스킬을 추가하고, `start-work` 자동 실행을 기능 동작에 영향을 주는 코드 작업으로 좁혔다.
- **결정:** 백로그에는 상태를 두지 않으며, 이 하네스 프로젝트에서는 ADR을 제거하고 과거 결정은 변경 로그로만 보존한다.

## 2026-08-15 — 리뷰 단위 커밋 절차 추가

- **의도:** 사용자가 작업 결과를 커밋 단위로 이해하고 리뷰할 수 있게 한다.
- **변경:** `start-work`에 커밋 계획, 안전한 스테이징, 논리 단위 커밋과 커밋별 완료 보고 절차를 추가했다.
- **결정:** 로컬 커밋 규칙을 우선하고, 별도 규칙이 없으면 Conventional Commits를 사용하며 push·브랜치·PR은 자동 수행하지 않는다.

## 2026-08-15 — 로컬 프로젝트 규칙 우선

- **의도:** 공통 하네스를 사용하더라도 프로젝트마다 다른 환경과 작업 관례를 보존한다.
- **변경:** 하네스와 대상 프로젝트의 규칙이 충돌하면 더 구체적인 로컬 규칙을 우선하도록 `AGENTS.md`에 우선순위를 추가했다.
- **결정:** 하네스는 기본값으로 취급하되 시스템·안전 정책과 현재 사용자의 명시적 요청은 로컬 설정이 덮어쓰지 않는다.

## 2026-08-15 — 작업 시작 인터뷰와 ADR 도입

- **의도:** 개발 전에 사용자와 컨텍스트를 충분히 맞추고, 주요 결정을 사용자 승인 아래 확정한다.
- **변경:** 변경 작업 전에 상세 인터뷰를 수행하는 `start-work` 스킬과 자동 실행 규칙을 추가하고, 승인 결과를 `docs/adr`에 남기는 규약을 마련했다.
- **결정:** 결과에 영향을 주는 결정을 우선 질문하되 경계는 실제 사용으로 조정하며, 구현 중 새 결정이 생겨도 사용자 확인 후 진행한다.

## 2026-08-15 — 초기 작업 환경 구성

- **의도:** 개인 작업 원칙과 반복 작업용 스킬을 한 저장소에서 관리하고, 이 저장소로 시작한 Codex 세션에 자동으로 제공한다.
- **변경:** 사용자 의도와 요청 범위를 우선하는 `AGENTS.md`, 로컬 `engineering-writeup` 스킬의 저장소 사본과 UI 메타데이터, 사용 방법을 설명하는 `README.md`를 추가했다.
- **결정:** 변경 기록은 당분간 단일 `CHANGELOG.md`에 간결하게 누적하고, 메이저 변경이나 문서 비대화가 발생한 뒤 분리를 검토한다.
